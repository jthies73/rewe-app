name: GitHub Actions Pipeline 🚀
run-name: ${{ github.actor }} is deploying rewe-app 🚀
on: [push]
jobs:
  Deploy-Rewe-Fe:
    runs-on: self-hosted
    steps:
      - run: ls -al
      - run: echo "Deploying rewe-fe 🚀"
  Deploy-Rewe-Be:
    runs-on: self-hosted
    steps:
      - run: ls -al
      - run: echo "Deploying rewe-be 🚀"
